apiVersion: v1
kind: Service
metadata:
  name: planee-marketing-web-svc
  namespace: planee
  labels:
    app: planee-marketing-web
spec:
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
  selector:
    app: planee-marketing-web
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: planee-marketing-web
  namespace: planee
  labels:
    app: planee-marketing-web
spec:
  replicas: 1
  selector:
    matchLabels:
      app: planee-marketing-web
  template:
    metadata:
      labels:
        app: planee-marketing-web
    spec:
      serviceAccountName: planee-app-sa
      initContainers:
        - name: wait-for-migration
          image: bitnami/kubectl:latest
          command:
            - sh
            - -c
            - |
              echo "Waiting for database migration job to complete..."
              kubectl wait --namespace=planee --for=condition=complete --timeout=300s job/planee-db-migrate
      containers:
        - name: web
          image: registry.cloud.martinpetr.dev/planee/marketing-web:sha-70f6fb7
          imagePullPolicy: Always
          ports:
            - containerPort: 3000
              name: http
          env:
            - name: PORT
              value: "3000"
            - name: AUTH_TRUST_HOST
              value: "true"
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
          resources:
            requests:
              cpu: "100m"
              memory: "256Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
          readinessProbe:
            tcpSocket:
              port: 3000
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            tcpSocket:
              port: 3000
            initialDelaySeconds: 15
            periodSeconds: 20
---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: planee-marketing-web-ingress
  namespace: planee
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`planee.martinpetr.dev`)
      kind: Rule
      services:
        - name: planee-marketing-web-svc
          port: 80
  tls:
    certResolver: cloudflare
