# Copy this into .env file and fill in the values

# -- API --
# Port on which the service should listen for HTTP requests
CONF_API_HTTP_PORT=4001
# Comma-separated list of origins that are allowed in CORS requests. ("*" for all)
CONF_API_HTTP_CORS_ORIGINS=http://localhost:4000
